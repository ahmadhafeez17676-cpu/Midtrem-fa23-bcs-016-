# Database Type: 'mongodb', 'mysql', or 'sqlite'
DB_TYPE=mysql

# MongoDB Configuration (required if DB_TYPE=mongodb)
MONGODB_URI=mongodb://localhost:27017/crud_app

# MySQL Configuration (required if DB_TYPE=mysql)
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=Ursoft@00001
MYSQL_DATABASE=crud_app
MYSQL_PORT=3306

# SQLite Configuration (optional, uses default if not specified)
SQLITE_DB_PATH=./database/crud_app.db

# Server Configuration
PORT=5000
NODE_ENV=development
